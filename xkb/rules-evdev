Change this:
  *             =   +inet(evdev)
to this:
  *             =   +inet(evdev)+workman-qwerty-ctrl(imaginary-qwerty)

Otherwise, our mappings just get overridden by the mappings in `symbols/inet`.
