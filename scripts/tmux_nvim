#!/bin/bash

# If not inside a tmux session, re-run from inside one.
if [[ -z "$TMUX" ]]; then
    exec tmux new-session "$0" "$@"
fi

# Hide the status bar.
tmux set-option status off

# Start nvim, with less clutter.
exec nvim -c 'set wrap cc= fcs=eob:\ ' "$@"
