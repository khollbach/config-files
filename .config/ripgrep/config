# Sort output by file path.
#
# Slows down searching, but it's easier to find what you're looking for.
--sort=path

# Ignore case by default. Use `-s` to make search case-sensitive.
--ignore-case

# Include hidden files and directories in the search by default.
--hidden

# Ignore .git directories, ctags tagfiles, etc.
--glob=!.git/
--glob=!.jj/
--glob=!.svn/
--glob=!.hg/
--glob=!tags
--glob=!compile_commands.json
--glob=!node_modules/

# Heed .gitignore even when not in a git repo, so that ripgrep works with jj.
#
# Once the following gets merged, we won't need this workaround any more:
# https://github.com/BurntSushi/ripgrep/pull/2842
--no-require-git
