general {
    colors = true
    interval = 1
}

order += "battery all"
order += "tztime local"

battery all {
    format = " %percentage "
    integer_battery_capacity = true
    last_full_capacity = true
}

tztime local {
    format = "%l:%M%P"
}
