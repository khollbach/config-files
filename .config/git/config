[user]
    name = Kevan Hollbach

# Use my personal email, except when at work.
[include]
    path = personal-email
    path = ~/work-configs/gitconfig

# Don't use my work email for this repo.
[includeIf "gitdir:~/.config-files/.git"]
    path = personal-email

[include]
    path = aliases
    path = delta

[init]
    defaultBranch = main

[merge]
	conflictstyle = diff3
